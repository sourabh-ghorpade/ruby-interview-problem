# frozen_string_literal: true

RSpec.describe Ruby::Interview::Problem do
  it "has a version number" do
    expect(Ruby::Interview::Problem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
